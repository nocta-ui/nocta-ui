---
title: Command Palette
description: A command palette modal (Cmd/Ctrl+K) for quick actions and navigation with search, grouping, and full keyboard accessibility
---

import { CommandK } from '@/app/components/ui/command-k';
import { BasicCommandKDemo } from '@/app/components/ui/command-k/command-k-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';
import { Step, Steps } from "fumadocs-ui/components/steps"

## Installation

<Steps>
  <Step>
    Install the Command Palette component using the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx nocta-ui add command-k
    ```
  </Step>
  <Step>
    Import the component:

    ```tsx lineNumbers title="Import"
    import { CommandK } from '@/components/ui/command-k';
    ```
  </Step>
</Steps>

## Example

### Default Command Palette

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicCommandKDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Command Palette"
     const items: CommandKItem[] = [
		{
			label: "Search docs",
			group: "Navigation",
			description: "Go to documentation",
			icon: IconSearch,
			shortcut: ["S"],
		},
		{
			label: "Open settings",
			group: "Navigation",
			description: "Application preferences",
			icon: IconCog,
			shortcut: ["O"],
		},
		{
			label: "New file",
			group: "Actions",
			description: "Create an untitled file",
			icon: IconFile,
			shortcut: ["Shift", "F"],
		},
		{
			label: "New folder",
			group: "Actions",
			description: "Create a folder",
			icon: IconFolder,
			shortcut: ["Shift", "G"],
		},
	];

  const [open, setOpen] = useState(false);

  <button onClick={() => setOpen(true)}>Open Command Palette</button>
  <CommandK
      items={items}
      open={open}
      onOpenChange={setOpen}
      onSelect={(it) => {
      toast.success({
        title: "Command executed",
        description: `Selected: ${it.label}`,
      });
    }}
  />;
    ```
  </DocsTab>
</DocsTabs>

## API Reference

### CommandK

<TypeTable
  type={{
    items: {
      description: 'List of entries to render',
      type: 'CommandKItem[]',
      default: 'undefined',
    },
    open: {
      description: 'Controlled open state',
      type: 'boolean',
      default: 'undefined',
    },
    defaultOpen: {
      description: 'Initial open state',
      type: 'boolean',
      default: 'false',
    },
    onOpenChange: {
      description: 'Callback when open state changes',
      type: '(open: boolean) => void',
      default: 'undefined',
    },
    placeholder: {
      description: 'Search input placeholder',
      type: 'string',
      default: '"Type a command or search..."',
    },
    emptyMessage: {
      description: 'Message when no results match',
      type: 'string',
      default: '"No results"',
    },
    closeOnAction: {
      description: 'Close palette after selection',
      type: 'boolean',
      default: 'true',
    },
    size: {
      description: 'Dialog size variant',
      type: '"sm" | "md" | "lg" | "xl" | "full"',
      default: '"xl"',
    },
    className: {
      description: 'Extra classes for DialogContent wrapper',
      type: 'string',
      default: '""',
    },
    listClassName: {
      description: 'Extra classes for results list',
      type: 'string',
      default: '""',
    },
    autoFocus: {
      description: 'Focus search input when opened',
      type: 'boolean',
      default: 'true',
    },
    hotkey: {
      description: 'Open shortcut; when no modifier specified, uses platform default (⌘/Ctrl)',
      type: '{ key: string; metaKey?: boolean; ctrlKey?: boolean; }',
      default: "{ key: 'k' }",
    },
    onSelect: {
      description: 'Called on item selection',
      type: '(item: CommandKItem) => void',
      default: 'undefined',
    },
  }}
/>

### CommandKItem

<TypeTable
  type={{
    id: {
      description: 'Optional unique ID',
      type: 'string',
      default: 'auto',
    },
    label: {
      description: 'Display label',
      type: 'string',
      default: 'undefined',
    },
    group: {
      description: 'Group header label',
      type: 'string',
      default: '"General"',
    },
    description: {
      description: 'Secondary description text',
      type: 'string',
      default: '""',
    },
    keywords: {
      description: 'Additional searchable terms',
      type: 'string[]',
      default: '[]',
    },
    icon: {
      description: 'Optional icon',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    shortcut: {
      description: 'Visual shortcut hints',
      type: 'string[]',
      default: '[]',
    },
    disabled: {
      description: 'Disable selection',
      type: 'boolean',
      default: 'false',
    },
    action: {
      description: 'Action executed on select',
      type: '() => void',
      default: 'undefined',
    },
  }}
/>
