---
title: Button
description: A versatile button component with multiple variants, sizes and dark mode support
---

import { Button } from '@/app/components/ui/button';
import { 
  PrimaryButtonDemo, 
  SecondaryButtonDemo, 
  GhostButtonDemo, 
  IconButtonDemo,
  SizesDemo, 
} from '@/app/components/ui/button/button-demos';
import { DocsTabs, DocsTab } from "@/app/components/ui/docs-tab"

## Installation

Install the Button component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx nocta-ui add button
```

Then import the component:

```tsx lineNumbers title="Import"
import { Button } from '@/components/ui/button';
```

## Examples

### Primary Button

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <PrimaryButtonDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Primary Button"
    <Button>Get Started</Button>
    ```
  </DocsTab>
</DocsTabs>

### Secondary Button

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SecondaryButtonDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Secondary Button"
    <Button variant="secondary">Learn More</Button>
    ```
  </DocsTab>
</DocsTabs>

### Ghost Button

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <GhostButtonDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Ghost Button"
    <Button variant="ghost">Cancel</Button>
    ```
  </DocsTab>
</DocsTabs>

### Icon Button

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <IconButtonDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Icon Button"
    {/* Icon-only buttons should provide an accessible name */}
    <Button variant="icon" size="sm" aria-label="Add">
      <PlusIcon className="w-4 h-4" />
    </Button>
    <Button variant="icon" size="md" aria-label="Download">
      <DownloadIcon className="w-5 h-5" />
    </Button>
    <Button variant="icon" size="lg" aria-label="Settings">
      <SettingsIcon className="w-6 h-6" />
    </Button>
    ```
  </DocsTab>
</DocsTabs>

## Sizes

Three size options are available: `sm`, `md`, and `lg`. The default size is `md`.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SizesDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Button Sizes"
    <Button size="sm">Small</Button>
    <Button size="md">Medium</Button>
    <Button size="lg">Large</Button>
    ```
  </DocsTab>
</DocsTabs>

## Api Reference

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `React.ReactNode` | — | Button content |
| `variant` | `'primary' \| 'secondary' \| 'ghost' \| 'icon'` | `'primary'` | Button style variant |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Button size |
| `className` | `string` | `''` | Additional CSS classes |
| `disabled` | `boolean` | `false` | Disable the button |
| `type` | `'button' \| 'submit' \| 'reset'` | `'button'` | Button type (defaults to button to avoid accidental form submit) |
| `aria-label` | `string` | — | Accessible name for icon-only buttons |
| `onClick` | `() => void` | — | Click handler |

The Button component also accepts all standard HTML button attributes. When `disabled` is set, the component also exposes `aria-disabled` for assistive technologies.