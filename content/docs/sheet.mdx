---
title: Sheet
description: A slide-out panel component that can be triggered from any side of the screen
---

import {
  BasicSheetDemo,
  SheetSidesDemo,
  SheetSizesDemo,
  ResizableSheetDemo,
  ControlledSheetDemo,
} from '@/demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';

## Installation

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
<Step>
Install the Sheet component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx @nocta-ui/cli add sheet
```
</Step>
<Step>
Import the component:

```tsx lineNumbers title="Import"
import {
  Sheet,
  SheetTrigger,
  SheetSurface,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetDescription,
  SheetFooter,
  SheetClose
} from '@/components/ui/sheet';
```
</Step>
</Steps>

## Examples

### Default Sheet

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicSheetDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Sheet"
    <Sheet>
      <SheetTrigger asChild>
        <Button>Open Sheet</Button>
      </SheetTrigger>
      <SheetSurface>
        <SheetHeader>
          <SheetTitle>Sheet Title</SheetTitle>
          <SheetDescription>
            This is a basic sheet panel.
          </SheetDescription>
        </SheetHeader>
        <SheetContent>
          <p className="text-sm text-foreground/70">
            Sheet content goes here. You can add any components or content you need.
          </p>
        </SheetContent>
        <SheetFooter>
          <SheetClose>Close</SheetClose>
        </SheetFooter>
      </SheetSurface>
    </Sheet>
    ```
  </DocsTab>
</DocsTabs>

### Sides

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SheetSidesDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Sheet Sides"
    <Sheet>
      <SheetTrigger asChild>
        <Button variant="secondary">Left Side</Button>
      </SheetTrigger>
      <SheetSurface side="left">
        <SheetHeader>
          <SheetTitle>Left Sheet</SheetTitle>
          <SheetDescription>
            This sheet slides in from the left side.
          </SheetDescription>
        </SheetHeader>
        <SheetContent>
          <p className="text-sm text-foreground/70">
            Content for left-side sheet.
          </p>
        </SheetContent>
      </SheetSurface>
    </Sheet>

    <Sheet>
      <SheetTrigger asChild>
        <Button variant="secondary">Right Side</Button>
      </SheetTrigger>
      <SheetSurface side="right">
        <SheetHeader>
          <SheetTitle>Right Sheet</SheetTitle>
          <SheetDescription>
            This sheet slides in from the right side.
          </SheetDescription>
        </SheetHeader>
        <SheetContent>
          <p className="text-sm text-foreground/70">
            Content for right-side sheet.
          </p>
        </SheetContent>
      </SheetSurface>
    </Sheet>

    {/* Top & Bottom sides work similarly */}
    ```
  </DocsTab>
</DocsTabs>

### Sizes

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SheetSizesDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Sheet Sizes"
    <Sheet>
      <SheetTrigger asChild>
        <Button variant="ghost" size="sm">Small</Button>
      </SheetTrigger>
      <SheetSurface size="sm">
        <SheetHeader>
          <SheetTitle>Small Sheet</SheetTitle>
          <SheetDescription>
            This is a small-sized sheet (320px width).
          </SheetDescription>
        </SheetHeader>
        <SheetContent>
          <p className="text-sm text-foreground/70">
            Compact content for small sheet.
          </p>
        </SheetContent>
      </SheetSurface>
    </Sheet>
    ```
  </DocsTab>
</DocsTabs>

### Resizable


<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <ResizableSheetDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Resizable Sheet"
    <Sheet>
      <SheetTrigger asChild>
        <Button variant="outline">Resizable Sheet</Button>
      </SheetTrigger>
      <SheetSurface resizable allowShrink size="lg">
        <SheetHeader>
          <SheetTitle>Resizable Panel</SheetTitle>
          <SheetDescription>
            Drag the divider to change the width.
          </SheetDescription>
        </SheetHeader>
        <SheetContent>
          <p className="text-sm text-foreground/70">
            Resizable sheets give power users extra control over the layout.
          </p>
        </SheetContent>
        <SheetFooter>
          <SheetClose>Close</SheetClose>
          <Button>Save layout</Button>
        </SheetFooter>
      </SheetSurface>
    </Sheet>
    ```
  </DocsTab>
</DocsTabs>

## Api Reference

### Sheet

<TypeTable
  type={{
    children: {
      description: 'Sheet content including trigger and content components',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    open: {
      description: 'Controlled open state',
      type: 'boolean',
      default: 'undefined',
    },
    onOpenChange: {
      description: 'Callback when open state changes',
      type: '(open: boolean) => void',
      default: 'undefined',
    },
  }}
/>

### SheetTrigger

<TypeTable
  type={{
    children: {
      description: 'Trigger content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    asChild: {
      description: 'Render as child element',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### SheetSurface

<TypeTable
  type={{
    children: {
      description: 'Sheet panel content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    side: {
      description: 'Side from which sheet slides in',
      type: '"left" | "right" | "top" | "bottom"',
      default: '"right"',
    },
    size: {
      description: 'Sheet size',
      type: '"sm" | "md" | "lg" | "xl" | "full"',
      default: '"md"',
    },
    showClose: {
      description: 'Show default close button',
      type: 'boolean',
      default: 'true',
    },
    resizable: {
      description: 'Display resize handles on the sheet edge',
      type: 'boolean',
      default: 'false',
    },
    allowShrink: {
      description: 'Permit resizing below the initial layout size',
      type: 'boolean',
      default: 'false',
    },
  }}
/>

### SheetContent

<TypeTable
  type={{
    children: {
      description: 'Scrollable content area inside the sheet',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### SheetHeader

<TypeTable
  type={{
    children: {
      description: 'Header content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### SheetTitle

<TypeTable
  type={{
    children: {
      description: 'Title content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### SheetDescription

<TypeTable
  type={{
    children: {
      description: 'Description content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### SheetFooter

<TypeTable
  type={{
    children: {
      description: 'Footer content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### SheetClose

<TypeTable
  type={{
    children: {
      description: 'Button content',
      type: 'React.ReactNode',
      default: '"Close"',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    asChild: {
      description: 'Render as child element',
      type: 'boolean',
      default: 'false',
    },
  }}
/>
