---
title: Form
description: A composable form component with field validation, labels, and actions for building accessible forms
---

import { Form, FormField, FormLabel, FormControl, FormDescription, FormMessage, FormActions, FormInput, FormSubmit } from '@/app/components/ui/form';
import {
  BasicFormDemo,
  FormWithErrorsDemo,
  ComplexFormDemo,
  FormActionsVariantsDemo,
  FormMessageTypesDemo,
} from '@/app/components/ui/form/form-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';
import { Step, Steps } from "fumadocs-ui/components/steps"

## Installation

<Steps>
  <Step>
    Install the Form component component using the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx @nocta-ui/cli add form
    ```
  </Step>
  <Step>
    Import the component:

    ```tsx lineNumbers title="Import"
    import {
      Form,
      FormField,
      FormLabel,
      FormControl,
      FormDescription,
      FormMessage,
      FormActions,
      FormInput,
      FormSubmit,
    } from '@/components/ui/form';
    ```
  </Step>
</Steps>

## Examples

### Default Form

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicFormDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Form"
    <Form onSubmit={handleSubmit}>
      <FormField name="name">
        <FormLabel required>Full Name</FormLabel>
        <FormControl>
          <Input placeholder="Enter your full name" />
        </FormControl>
        <FormDescription>This will be displayed on your profile.</FormDescription>
      </FormField>

      <FormField name="email">
        <FormLabel required>Email</FormLabel>
        <FormControl>
          <Input type="email" placeholder="Enter your email" />
        </FormControl>
      </FormField>

      <FormActions>
        <Button type="submit">Submit</Button>
      </FormActions>
    </Form>
    ```
  </DocsTab>
</DocsTabs>


## Error Handling

Handle validation in two ways:
- Pass an `error` prop to `FormField` and place `<FormMessage />` to display it.
- Or use an Ariakit form store with `validate`/`setError` and place `<FormMessage type="error" />` to read errors from the store.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <FormWithErrorsDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Form with Errors"
    const [errors, setErrors] = useState({
      username: 'Username is already taken',
      password: ''
    });

    <Form>
      <FormField name="username" error={errors.username}>
        <FormLabel required>Username</FormLabel>
        <FormControl>
          <Input placeholder="Choose a username" />
        </FormControl>
        <FormMessage />
      </FormField>

      <FormField name="password" error={errors.password}>
        <FormLabel required>Password</FormLabel>
        <FormControl>
          <Input type="password" placeholder="Create a password" />
        </FormControl>
        <FormDescription>Must be at least 8 characters long.</FormDescription>
        <FormMessage />
      </FormField>
    </Form>
    ```
  </DocsTab>
</DocsTabs>

## Api Reference

### Form

<TypeTable
  type={{
    children: {
      description: 'Form content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    onSubmit: {
      description: 'Form submit handler',
      type: '(event: React.FormEvent) => void',
      default: 'undefined',
    },
    store: {
      description: 'Optional Ariakit form store',
      type: 'FormStore',
      default: 'useFormStore({})',
    },
  }}
/>

### FormField

<TypeTable
  type={{
    children: {
      description: 'Field content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    name: {
      description: 'Field name identifier',
      type: 'string',
      default: 'undefined',
    },
    error: {
      description: 'Error message to display',
      type: 'string',
      default: 'undefined',
    },
    description: {
      description: 'Description text',
      type: 'string',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### FormLabel

<TypeTable
  type={{
    children: {
      description: 'Label content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    required: {
      description: 'Show required indicator',
      type: 'boolean',
      default: 'false',
    },
  }}
/>

### FormMessage

<TypeTable
  type={{
    children: {
      description: 'Message content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    type: {
      description: 'Message type',
      type: '"error" | "success" | "warning"',
      default: '"error"',
    },
  }}
/>

### FormActions

<TypeTable
  type={{
    children: {
      description: 'Action buttons',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    align: {
      description: 'Button alignment',
      type: '"left" | "center" | "right"',
      default: '"right"',
    },
  }}
/>

### FormInput

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    '...inputProps': {
      description: 'Native input props forwarded to Ariakit FormInput',
      type: 'InputHTMLAttributes<HTMLInputElement>',
      default: 'undefined',
    },
  }}
/>

### FormSubmit

<TypeTable
  type={{
    children: {
      description: 'Button content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    '...buttonProps': {
      description: 'Native button props forwarded to Ariakit FormSubmit',
      type: 'ButtonHTMLAttributes<HTMLButtonElement>',
      default: 'undefined',
    },
  }}
/>
