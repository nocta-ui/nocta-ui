---
title: Spinner
description: Loading spinner component with variants, sizes and smooth animations for indicating loading states
---

import { Spinner } from '@/app/components/ui/spinner';
import { 
  BasicSpinnerDemo,
  SizesDemo,
  LoadingButtonDemo,
  InlineSpinnerDemo,
  CenteredSpinnerDemo,
  CustomColorDemo
} from '@/app/components/ui/spinner/spinner-demos';
import { DocsTabs, DocsTab } from "@/app/components/ui/docs-tab"

## Installation

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
<Step>
Install the Spinner component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx nocta-ui add spinner
```
</Step>
<Step>
Import the component:

```tsx lineNumbers title="Import"
import { Spinner } from '@/components/ui/spinner';
```
</Step>
</Steps>

## Basic Usage

### Default Spinner

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicSpinnerDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Default Spinner"
    <Spinner />
    ```
  </DocsTab>
</DocsTabs>

## Sizes

Three size options are available: `sm`, `md`, and `lg`. The default size is `md`.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SizesDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Spinner Sizes"
    <Spinner size="sm" />
    <Spinner size="md" />
    <Spinner size="lg" />
    ```
  </DocsTab>
</DocsTabs>

## Customization

### Custom Colors

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <CustomColorDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Custom Colors"
    <Spinner className="text-blue-500 dark:text-blue-500/50" />
    <Spinner className="text-green-500 dark:text-green-500/50" />
    <Spinner className="text-red-500 dark:text-red-500/50" />
    <Spinner className="text-purple-500 dark:text-purple-500/50" />
    <Spinner className="text-orange-500 dark:text-orange-500/50" />
    ```
  </DocsTab>
</DocsTabs>

### Custom Styling

The Spinner component accepts a `className` prop for custom styling:

```tsx lineNumbers title="Custom Styling"
<Spinner className="w-10 h-10 border-4" />

<Spinner className="text-indigo-600 animate-pulse" />

<Spinner className="border-gradient-to-r from-purple-400 to-pink-400" />
```

## Accessibility

The Spinner component includes proper accessibility attributes:

- `role="status"` for screen readers
- `aria-label="Loading"` to describe the loading state  
- Hidden "Loading..." text for screen readers via `sr-only`

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Spinner size |
| `variant` | `'default' \| 'primary' \| 'secondary'` | `'default'` | Spinner style variant |
| `className` | `string` | `''` | Additional CSS classes |

The Spinner component also accepts all standard HTML div attributes.