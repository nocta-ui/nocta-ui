---
title: Dialog
description: A modal dialog component for displaying content over the main interface with backdrop, keyboard navigation and accessibility features
---

import { Dialog, DialogTrigger, DialogSurface, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogActions, DialogClose } from '@/app/components/ui/dialog';
import { Button } from '@/app/components/ui/button';
import { Input } from '@/app/components/ui/input';
import { Select, SelectTrigger, SelectContent, SelectItem, SelectValue } from '@/app/components/ui/select';
import {
  SimpleDialogDemo,
  DialogWithActionsDemo,
  SizesDemo
} from '@/app/components/ui/dialog/dialog-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';
import { Step, Steps } from "fumadocs-ui/components/steps"

## Installation

<Steps>
  <Step>
    Install the Dialog component using the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx @nocta-ui/cli add dialog
    ```
  </Step>
  <Step>
    Import the components you need:

    ```tsx lineNumbers title="Import"
    import {
      Dialog,
      DialogTrigger,
      DialogSurface,
      DialogContent,
      DialogHeader,
      DialogTitle,
      DialogDescription,
      DialogFooter,
      DialogActions,
      DialogClose,
    } from '@/components/ui/dialog';
    ```
  </Step>
</Steps>

## Examples

### Default Dialog

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SimpleDialogDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Simple Dialog"
    <Dialog>
      <DialogTrigger asChild>
        <Button>Open Dialog</Button>
      </DialogTrigger>
      <DialogSurface>
        <DialogHeader>
          <DialogTitle>Welcome</DialogTitle>
          <DialogDescription>
            This is a basic dialog example with a title and description.
          </DialogDescription>
        </DialogHeader>
        <DialogContent>
          <p className="text-sm">Dialog content goes here.</p>
        </DialogContent>
      </DialogSurface>
    </Dialog>
    ```
  </DocsTab>
</DocsTabs>

### Dialog with Actions

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <DialogWithActionsDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Dialog with Actions"
    <Dialog>
      <DialogTrigger asChild>
        <Button variant="secondary">Delete Item</Button>
      </DialogTrigger>
      <DialogSurface showClose={false}>
        <DialogHeader>
          <DialogTitle>Confirm Deletion</DialogTitle>
          <DialogDescription>
            Are you sure you want to delete this item?
          </DialogDescription>
        </DialogHeader>
        <DialogContent>
          <p className="text-sm">
            This will permanently remove <strong>Project Alpha</strong> and all associated data.
          </p>
        </DialogContent>
        <DialogFooter>
          <DialogActions>
            <DialogClose>
                Cancel
            </DialogClose>
            <Button variant="default" size="sm">Delete</Button>
          </DialogActions>
        </DialogFooter>
      </DialogSurface>
    </Dialog>
    ```
  </DocsTab>
</DocsTabs>

### Sizes

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SizesDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Dialog Sizes"
    <Dialog>
      <DialogTrigger asChild>
        <Button size="sm">Small</Button>
      </DialogTrigger>
      <DialogSurface size="sm">
        <DialogHeader>
          <DialogTitle>Small Dialog</DialogTitle>
          <DialogDescription>This is a small dialog example.</DialogDescription>
        </DialogHeader>
        <DialogContent>
          <p className="text-sm">Compact dialog for simple interactions.</p>
        </DialogContent>
      </DialogSurface>
    </Dialog>
    ```
  </DocsTab>
</DocsTabs>

## API Reference

### Dialog

<TypeTable
  type={{
    open: {
      description: 'Controls the dialog state',
      type: 'boolean',
      default: 'undefined',
    },
    onOpenChange: {
      description: 'Callback when dialog state changes',
      type: '(open: boolean) => void',
      default: 'undefined',
    },
    children: {
      description: 'Dialog content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogTrigger

<TypeTable
  type={{
    asChild: {
      description: 'Render as child element instead of button',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Trigger content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogSurface

<TypeTable
  type={{
    size: {
      description: 'Dialog size',
      type: '"sm" | "md" | "lg" | "xl"',
      default: '"md"',
    },
    showClose: {
      description: 'Show close button',
      type: 'boolean',
      default: 'true',
    },
    portal: {
      description: 'Render the dialog in a React portal',
      type: 'boolean',
      default: 'true',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Dialog content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogContent

<TypeTable
  type={{
    children: {
      description: 'Scrollable body inside the dialog',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
  }}
/>

### DialogClose

<TypeTable
  type={{
    asChild: {
      description: 'Render as child element instead of button',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Close button content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogHeader

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Header content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogTitle

<TypeTable
  type={{
    as: {
      description: 'Optional element type for the heading',
      type: 'React.ElementType',
      default: '"h2"',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Title content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogDescription

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Description content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogFooter

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Footer content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### DialogActions

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Actions content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>
