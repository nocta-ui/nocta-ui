---
title: Menubar
description: A top-level application menubar with grouped actions, submenus, and full keyboard navigation
---

import {
  BasicMenubarDemo,
  MenubarDisabledDemo,
} from '@/demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab";
import { TypeTable } from '@/components/type-table';
import { Step, Steps } from "fumadocs-ui/components/steps";

## Installation

<Steps>
  <Step>
    Install the Menubar component with the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx @nocta-ui/cli add menubar
    ```
  </Step>
  <Step>
    Import only the pieces you need:

    ```tsx lineNumbers title="Import"
    import {
      Menubar,
      MenubarMenu,
      MenubarTrigger,
      MenubarContent,
    } from '@/components/ui/menubar';
    ```
  </Step>
</Steps>

## Examples

### Default Menubar

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicMenubarDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Menubar"
    const [lastAction, setLastAction] = useState('No action selected');

    <Menubar>
      <MenubarMenu>
        <MenubarTrigger>File</MenubarTrigger>
        <MenubarContent>
          <MenubarItem onClick={() => setLastAction("New project")}>
            <FilePlusIcon aria-hidden="true" />
            New project
          </MenubarItem>
          <MenubarItem onClick={() => setLastAction("Open file")}>
            <OpenInNewWindowIcon aria-hidden="true" />
            Open file
          </MenubarItem>
          <MenubarItem onClick={() => setLastAction("Save")}>
            <EnterIcon aria-hidden="true" />
            Save
          </MenubarItem>
          <MenubarSeparator />
          <MenubarSub>
            <MenubarSubTrigger>
              <Share1Icon aria-hidden="true" />
              Share
            </MenubarSubTrigger>
            <MenubarSubContent>
              <MenubarItem onClick={() => setLastAction("Share to email")}>
                Email
              </MenubarItem>
              <MenubarItem onClick={() => setLastAction("Share to teams")}>
                Teams
              </MenubarItem>
              <MenubarItem onClick={() => setLastAction("Copy link")}>
                Copy link
              </MenubarItem>
            </MenubarSubContent>
          </MenubarSub>
          <MenubarSeparator />
          <MenubarItem destructive onClick={() => setLastAction("Close window")}>
            Close window
          </MenubarItem>
        </MenubarContent>
      </MenubarMenu>

      <MenubarMenu>
        <MenubarTrigger>Edit</MenubarTrigger>
        <MenubarContent>
          <MenubarItem onClick={() => setLastAction("Undo")}>
            <ReloadIcon aria-hidden="true" />
            Undo
          </MenubarItem>
          <MenubarItem onClick={() => setLastAction("Redo")}>
            <ReloadIcon aria-hidden="true" className="rotate-180" />
            Redo
          </MenubarItem>
          <MenubarSeparator />
          <MenubarItem onClick={() => setLastAction("Cut")}>
            <ScissorsIcon aria-hidden="true" />
            Cut
          </MenubarItem>
          <MenubarItem onClick={() => setLastAction("Copy")}>
            <CopyIcon aria-hidden="true" />
            Copy
          </MenubarItem>
          <MenubarItem onClick={() => setLastAction("Paste")}>
            <DotsHorizontalIcon aria-hidden="true" />
            Paste
          </MenubarItem>
        </MenubarContent>
      </MenubarMenu>
    </Menubar>
    ```
  </DocsTab>
</DocsTabs>

### Disabled States

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <MenubarDisabledDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Disabled Menubar Items"
    <Menubar>
      <MenubarMenu>
        <MenubarTrigger>File</MenubarTrigger>
        <MenubarContent>
          <MenubarItem>New file</MenubarItem>
          <MenubarItem disabled>Save (synced)</MenubarItem>
          <MenubarSeparator />
          <MenubarItem>Export</MenubarItem>
          <MenubarItem disabled>Close (background task)</MenubarItem>
        </MenubarContent>
      </MenubarMenu>

      <MenubarMenu>
        <MenubarTrigger disabled>Team</MenubarTrigger>
      </MenubarMenu>
    </Menubar>
    ```
  </DocsTab>
</DocsTabs>

## API Reference

### Menubar

<TypeTable
  type={{
    children: {
      description: 'Menubar menus',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional styling',
      type: 'string',
      default: 'undefined',
    },
    size: {
      description: 'Control menubar height',
      type: '"md"',
      default: '"md"',
    },
  }}
/>

### MenubarMenu

<TypeTable
  type={{
    children: {
      description: 'Trigger and content for a menu',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### MenubarTrigger

<TypeTable
  type={{
    children: {
      description: 'Visible trigger label',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Custom classes',
      type: 'string',
      default: 'undefined',
    },
    disabled: {
      description: 'Disable opening the menu',
      type: 'boolean',
      default: 'false',
    },
    size: {
      description: 'Trigger padding and height',
      type: '"md"',
      default: '"md"',
    },
  }}
/>

### MenubarContent

<TypeTable
  type={{
    children: {
      description: 'Menu body content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional classes',
      type: 'string',
      default: 'undefined',
    },
    size: {
      description: 'Adjusts padding and width',
      type: '"md"',
      default: '"md"',
    },
  }}
/>

### MenubarItem

<TypeTable
  type={{
    children: {
      description: 'Command label and accessories',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional classes',
      type: 'string',
      default: 'undefined',
    },
    disabled: {
      description: 'Disable interaction',
      type: 'boolean',
      default: 'false',
    },
    inset: {
      description: 'Indent the item',
      type: 'boolean',
      default: 'false',
    },
    destructive: {
      description: 'Apply destructive styling',
      type: 'boolean',
      default: 'false',
    },
    onClick: {
      description: 'Click handler',
      type: '() => void',
      default: 'undefined',
    },
  }}
/>

### MenubarSeparator

<TypeTable
  type={{
    className: {
      description: 'Additional classes',
      type: 'string',
      default: 'undefined',
    },
  }}
/>

### MenubarSub

<TypeTable
  type={{
    children: {
      description: 'Submenu trigger and content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### MenubarSubTrigger

<TypeTable
  type={{
    children: {
      description: 'Submenu trigger label',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional classes',
      type: 'string',
      default: 'undefined',
    },
    disabled: {
      description: 'Disable the submenu trigger',
      type: 'boolean',
      default: 'false',
    },
    inset: {
      description: 'Indent the trigger',
      type: 'boolean',
      default: 'false',
    },
    destructive: {
      description: 'Destructive styling',
      type: 'boolean',
      default: 'false',
    },
  }}
/>

### MenubarSubContent

<TypeTable
  type={{
    children: {
      description: 'Submenu body',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional classes',
      type: 'string',
      default: 'undefined',
    },
    size: {
      description: 'Adjusts submenu padding',
      type: '"md"',
      default: '"md"',
    },
  }}
/>
