---
title: Switch
description: A toggle switch component for binary choices with multiple variants, sizes and dark mode support
---

import { Switch } from '@/app/components/ui/switch';
import {
  BasicSwitchDemo,
  SizesDemo,
} from '@/app/components/ui/switch/switch-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';

## Installation

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
<Step>
Install the Switch component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx @nocta-ui/cli add switch
```
</Step>
<Step>
Import the component:

```tsx lineNumbers title="Import"
import { Switch } from '@/components/ui/switch';
```
</Step>
</Steps>

## Examples

### Default Switch

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6">
      <BasicSwitchDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Switch"
    const [isChecked, setIsChecked] = useState(false);

    <Switch
      checked={isChecked}
      onCheckedChange={setIsChecked}
    />
    ```
  </DocsTab>
</DocsTabs>

### Sizes

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6">
      <SizesDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Switch Sizes"
    <Switch size="sm" checked={true} />
    <Switch size="md" checked={true} />
    <Switch size="lg" checked={true} />
    ```
  </DocsTab>
</DocsTabs>

## API Reference

<TypeTable
  type={{
    checked: {
      description: 'Whether the switch is checked (controlled)',
      type: 'boolean',
      default: 'false',
    },
    defaultChecked: {
      description: 'Initial checked state (uncontrolled)',
      type: 'boolean',
      default: 'false',
    },
    onCheckedChange: {
      description: 'Callback fired when the switch state changes',
      type: '(checked: boolean) => void',
      default: 'undefined',
    },
    size: {
      description: 'The size of the switch',
      type: '"sm" | "md" | "lg"',
      default: '"md"',
    },
    disabled: {
      description: 'Whether the switch is disabled',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    id: {
      description: 'The id attribute for the switch input',
      type: 'string',
      default: 'undefined',
    },
  }}
/>
