---
title: Radio Group
description: A lightweight and accessible radio group component for selecting a single option, supporting labels, descriptions, and full keyboard navigation
---

import {
  BasicRadioGroupDemo,
  DescriptiveRadioGroupDemo,
  HorizontalRadioGroupDemo,
} from '@/demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';
import { Step, Steps } from "fumadocs-ui/components/steps"

## Installation

<Steps>
  <Step>
    Install the Radio Group component using the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx @nocta-ui/cli add radio-group
    ```
  </Step>
  <Step>
    Import the component:

    ```tsx lineNumbers title="Import"
    import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';
    ```
  </Step>
</Steps>

## Examples

### Default Radio Group

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6 space-y-3">
      <BasicRadioGroupDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Radio Group"
    const [theme, setTheme] = useState('system');

    <RadioGroup
      value={theme}
      onValueChange={(v) => setTheme(v as string)}
      aria-label="Theme"
    >
      <RadioGroupItem value="light" label="Light" />
      <RadioGroupItem value="dark" label="Dark" />
      <RadioGroupItem value="system" label="System" />
    </RadioGroup>
    ```
  </DocsTab>
</DocsTabs>

### Radio Group with Descriptions

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6 space-y-3">
      <DescriptiveRadioGroupDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Radio Group with Descriptions"
    const [plan, setPlan] = useState('pro');

    <RadioGroup
      value={plan}
      onValueChange={(v) => setPlan(v as string)}
      aria-label="Pricing plans"
    >
      <RadioGroupItem
        value="starter"
        label="Starter"
        description="Basic plan for personal projects"
      />
      <RadioGroupItem
        value="pro"
        label="Pro"
        description="Advanced features for growing teams"
      />
      <RadioGroupItem
        value="enterprise"
        label="Enterprise"
        description="Full access with dedicated support"
      />
    </RadioGroup>
    ```
  </DocsTab>
</DocsTabs>

### Horizontal Layout

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6">
      <HorizontalRadioGroupDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Horizontal Radio Group"
    const [alignment, setAlignment] = useState('center');

    <RadioGroup
      value={alignment}
      onValueChange={(v) => setAlignment(v as string)}
      className="flex flex-row gap-4"
      aria-label="Alignment"
    >
      <RadioGroupItem value="start" label="Start" />
      <RadioGroupItem value="center" label="Center" />
      <RadioGroupItem value="end" label="End" />
    </RadioGroup>
    ```
  </DocsTab>
</DocsTabs>

## API Reference

### RadioGroup

<TypeTable
  type={{
    value: {
      description: 'Controlled value of the selected option',
      type: 'string',
      default: 'undefined',
    },
    defaultValue: {
      description: 'Initial selected value for uncontrolled usage',
      type: 'string',
      default: 'undefined',
    },
    onValueChange: {
      description: 'Callback fired when the selection changes',
      type: '(value: string | number | null) => void',
      default: 'undefined',
    },
    disabled: {
      description: 'Disables selection for all radio items',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes applied to the group wrapper',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'One or more `RadioGroupItem` components',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### RadioGroupItem

<TypeTable
  type={{
    value: {
      description: 'Value associated with the radio option',
      type: 'string',
      default: 'undefined',
    },
    label: {
      description: 'Main label displayed next to the indicator',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    description: {
      description: 'Optional helper text below the label',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    disabled: {
      description: 'Disables this individual option',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes applied to the label wrapper',
      type: 'string',
      default: '""',
    },
  }}
/>
