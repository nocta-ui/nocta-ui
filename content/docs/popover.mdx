---
title: Popover
description: Advanced popover component with smart positioning, collision detection, and smooth animations for displaying rich content
---

import { Popover, PopoverTrigger, PopoverContent } from '@/app/components/ui/popover';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import {
  BasicPopoverDemo,
  SidesDemo,
  AlignmentDemo,
  OffsetsDemo,
  AsChildDemo,
  ControlledDemo,
  RichContentDemo,
  CollisionDetectionDemo
} from '@/app/components/ui/popover/popover-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';

## Installation

<Steps>
<Step>
Install the Popover component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx @nocta-ui/cli add popover
```
</Step>
<Step>
Import the component:

```tsx lineNumbers title="Import"
import {
  Popover,
  PopoverTrigger,
  PopoverContent
} from '@/components/ui/popover';
```
</Step>
</Steps>

## Example

### Default Popover

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicPopoverDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Popover"
    <Popover>
      <PopoverTrigger>Open Popover</PopoverTrigger>
      <PopoverContent>
        <div className="flex flex-col space-y-1 max-w-xs">
          <PopoverHeading>About this feature</PopoverHeading>
          <PopoverDescription>
            This is a popover component that can contain any content you want.
          </PopoverDescription>
        </div>
      </PopoverContent>
    </Popover>;
    ```
  </DocsTab>
</DocsTabs>

## Api Reference

### Popover

<TypeTable
  type={{
    open: {
      description: 'Controlled open state',
      type: 'boolean',
      default: 'undefined',
    },
    defaultOpen: {
      description: 'Default open state',
      type: 'boolean',
      default: 'false',
    },
    onOpenChange: {
      description: 'Callback when open state changes',
      type: '(open: boolean) => void',
      default: 'undefined',
    },
    gutter: {
      description: 'Default distance between the trigger and any PopoverContent',
      type: 'number',
      default: '0',
    },
    portal: {
      description: 'Whether PopoverContent renders inside a portal by default',
      type: 'boolean',
      default: 'true',
    },
    fixed: {
      description: 'Whether PopoverContent uses `position: fixed` by default',
      type: 'boolean',
      default: 'false',
    },
    showArrow: {
      description: 'Whether PopoverContent renders the arrow indicator by default',
      type: 'boolean',
      default: 'true',
    },
    placement: {
      description: 'Preferred placement of the popover relative to the trigger',
      type: '"top" | "top-start" | "top-end" | "bottom" | "bottom-start" | "bottom-end" | "left" | "left-start" | "left-end" | "right" | "right-start" | "right-end"',
      default: '"bottom"',
    },
  }}
/>

### PopoverTrigger

<TypeTable
  type={{
    variant: {
      description: 'Visual style of the trigger',
      type: '"default"',
      default: '"default"',
    },
    size: {
      description: 'Trigger size',
      type: '"sm" | "md" | "lg"',
      default: '"md"',
    },
    asChild: {
      description: 'Render as child element instead of button',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
  }}
/>

### PopoverContent

<TypeTable
  type={{
    variant: {
      description: 'Visual style variant',
      type: '"default"',
      default: '"default"',
    },
    size: {
      description: 'Content size (padding, typography)',
      type: '"sm" | "md" | "lg"',
      default: '"md"',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Content rendered inside the popover',
      type: 'React.ReactNode',
      default: 'required',
    },
  }}
/>

### PopoverHeading

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Heading content for the popover',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### PopoverDescription

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    children: {
      description: 'Descriptive text for supplementary context',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>
