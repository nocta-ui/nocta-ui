---
title: Slider
description: A customizable range slider component with variants, orientations, and accessibility support for value selection
---

import { Slider } from '@/app/components/ui/slider';
import {
  BasicSliderDemo,
  SecondarySliderDemo,
  SizesDemo,
  WithValueDemo,
  CustomRangeDemo,
  DisabledDemo,
  StepDemo,
  VerticalDemo,
  PriceRangeDemo
} from '@/app/components/ui/slider/slider-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';

## Installation

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
<Step>
Install the Slider component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx nocta-ui add slider
```
</Step>
<Step>
Import the component:

```tsx lineNumbers title="Import"
import { Slider } from '@/components/ui/slider';
```
</Step>
</Steps>

## Examples

### Default Slider

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicSliderDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Default Slider"
    const [value, setValue] = useState(50);

    <Slider
      value={value}
      onChange={setValue}
      aria-label="Basic slider"
    />
    ```
  </DocsTab>
</DocsTabs>

### Secondary Slider

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SecondarySliderDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Secondary Slider"
    <Slider
      variant="secondary"
      value={value}
      onChange={setValue}
    />
    ```
  </DocsTab>
</DocsTabs>

### Sizes

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SizesDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Slider Sizes"
    <Slider size="sm" value={value} onChange={setValue} />
    <Slider size="md" value={value} onChange={setValue} />
    <Slider size="lg" value={value} onChange={setValue} />
    ```
  </DocsTab>
</DocsTabs>

### Show Value

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <WithValueDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Show Value"
    <Slider
      value={value}
      onChange={setValue}
      showValue
      variant="default"
    />
    ```
  </DocsTab>
</DocsTabs>

### Custom Range & Formatting

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <CustomRangeDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Custom Range & Formatting"
    <Slider
      min={16}
      max={30}
      step={0.5}
      value={temperature}
      onChange={setTemperature}
      showValue
      formatValue={(val) => `${val}°C`}
      variant="default"
    />

    <Slider
      min={0}
      max={10}
      value={volume}
      onChange={setVolume}
      showValue
      variant="secondary"
    />
    ```
  </DocsTab>
</DocsTabs>

### Step Values

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <StepDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Step Values"
    <Slider
      min={0}
      max={100}
      step={5}
      value={value}
      onChange={setValue}
      showValue
      variant="default"
    />
    ```
  </DocsTab>
</DocsTabs>

### Vertical Orientation

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <VerticalDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Vertical Orientation"
    <Slider
      orientation="vertical"
      value={value}
      onChange={setValue}
      variant="default"
      className="h-48"
    />
    ```
  </DocsTab>
</DocsTabs>

### Disabled State

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <DisabledDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Disabled State"
    <Slider
      value={60}
      disabled
    />
    ```
  </DocsTab>
</DocsTabs>

## Api Reference

<TypeTable
  type={{
    value: {
      description: 'Controlled value',
      type: 'number',
      default: 'undefined',
    },
    defaultValue: {
      description: 'Default value for uncontrolled usage',
      type: 'number',
      default: '0',
    },
    min: {
      description: 'Minimum value',
      type: 'number',
      default: '0',
    },
    max: {
      description: 'Maximum value',
      type: 'number',
      default: '100',
    },
    step: {
      description: 'Step increment',
      type: 'number',
      default: '1',
    },
    disabled: {
      description: 'Disable the slider',
      type: 'boolean',
      default: 'false',
    },
    size: {
      description: 'Slider size',
      type: '"sm" | "md" | "lg"',
      default: '"md"',
    },
    variant: {
      description: 'Slider style variant',
      type: '"default" | "secondary"',
      default: '"default"',
    },
    orientation: {
      description: 'Slider orientation',
      type: '"horizontal" | "vertical"',
      default: '"horizontal"',
    },
    showValue: {
      description: 'Show current value',
      type: 'boolean',
      default: 'false',
    },
    formatValue: {
      description: 'Custom value formatter',
      type: '(value: number) => string',
      default: '(value) => value.toString()',
    },
    onChange: {
      description: 'Value change handler',
      type: '(value: number) => void',
      default: 'undefined',
    },
    onValueCommit: {
      description: 'Callback when value is committed',
      type: '(value: number) => void',
      default: 'undefined',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
      default: '""',
    },
    trackClassName: {
      description: 'Track element CSS classes',
      type: 'string',
      default: '""',
    },
    thumbClassName: {
      description: 'Thumb element CSS classes',
      type: 'string',
      default: '""',
    },
    'aria-label': {
      description: 'Accessibility label',
      type: 'string',
      default: 'undefined',
    },
    'aria-labelledby': {
      description: 'ID of labeling element',
      type: 'string',
      default: 'undefined',
    },
  }}
/>
