---
title: Table
description: Fully accessible data table powered by TanStack Table and Ariakit composites.
---

import { DocsTabs, DocsTab } from "@/components/docs-tab";
import { Step, Steps } from 'fumadocs-ui/components/steps';

import {
  BasicTableDemo,
  SelectableTableDemo,
  CompactTableDemo
} from '@/app/components/ui/table';

## Installation

<Steps>
  <Step>
    Install the Table component using the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx nocta-ui add table
    ```
  </Step>
  <Step>
    Then import the component and supporting types:

    ```tsx lineNumbers title="Import"
    import { Table, type ColumnDef } from '@/components/ui/table';
    ```
  </Step>
</Steps>

## Examples

### Basic Table

Display structured data with sorting and custom cell rendering.

<DocsTabs justify="start"  defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <BasicTableDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Table"
    import { Table, type ColumnDef } from '@/components/ui/table';

    interface Invoice {
      invoice: string;
      customer: string;
      status: 'paid' | 'processing' | 'overdue';
      issuedOn: Date;
      amount: number;
    }

    const currency = new Intl.NumberFormat('en-US', {
      style: 'currency',
      currency: 'USD',
      minimumFractionDigits: 0,
    });

    const columns: ColumnDef<Invoice>[] = [
      {
        accessorKey: 'invoice',
        header: 'Invoice',
        cell: ({ row }) => (
          <span className="font-medium">{row.getValue('invoice')}</span>
        ),
        meta: { width: '140px' },
      },
      {
        accessorKey: 'customer',
        header: 'Customer',
      },
      {
        accessorKey: 'issuedOn',
        header: 'Issued',
        cell: ({ getValue }) => {
          const date = getValue<Date>();
          return date.toLocaleDateString('en-US', {
            month: 'short',
            day: 'numeric',
            year: 'numeric',
          });
        },
        meta: { align: 'right', width: '160px' },
      },
      {
        accessorKey: 'amount',
        header: 'Amount',
        cell: ({ getValue }) => (
          <span className="font-medium">
            {currency.format(getValue<number>())}
          </span>
        ),
        meta: { align: 'right', width: '140px' },
      },
    ];

    const data: Invoice[] = [
      {
        invoice: 'INV-2041',
        customer: 'Lumen Studio',
        status: 'paid',
        issuedOn: new Date('2024-02-14'),
        amount: 2450,
      },
      // ...
    ];

    export default function BasicTableExample() {
      return (
        <Table<Invoice>
          columns={invoiceColumns}
          data={invoiceData}
          getRowId={(invoice) => invoice.invoice}
          density="comfortable"
		/>
      );
    }
    ```
  </DocsTab>
</DocsTabs>

### Row Selection

Enable checkbox selection with controlled state for bulk actions.

<DocsTabs justify="start" defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SelectableTableDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Row Selection"
    import * as React from 'react';
    import {
      Table,
      type ColumnDef,
      type Row,
    } from '@/components/ui/table';

    // columns and data defined as in the previous example
    const [selectedRows, setSelectedRows] = React.useState<Row<Invoice>[]>([]);

    <Table
			columns={invoiceColumns}
			data={invoiceData}
			getRowId={(invoice) => invoice.invoice}
			enableRowSelection
			onSelectedRowsChange={setSelectedRows}
			emptyState="No invoices yet"
    />;
    ```
  </DocsTab>
</DocsTabs>

### Compact Density

Switch the visual density for dashboards or sidebars.

<DocsTabs justify="start" defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <CompactTableDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Compact Table"
    // Reuse your existing column and data definitions
    const teamColumns: ColumnDef<TeamMember>[] = [...];
    const teamMembers: TeamMember[] = [...];

    <Table
      columns={teamColumns}
      data={teamMembers}
      getRowId={(member) => member.name}
      density="compact"
    />
    ```
  </DocsTab>
</DocsTabs>

## Column metadata

Set column-level `meta` options to align content or pin widths:

```tsx lineNumbers title="Column Meta"
const columns: ColumnDef<Invoice>[] = [
  {
    accessorKey: 'amount',
    header: 'Amount',
    meta: {
      align: 'right',
      width: '140px',
      headerTooltip: 'Total amount due',
    },
  },
];
```

`Table` exposes fully typed helpers from TanStack Table so you can compose advanced features like column visibility, custom filter functions, or pagination while keeping the default Nocta UI styling and keyboard support powered by Ariakit Composite.

## API Reference

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `columns` | `ColumnDef<TData, unknown>[]` | - | Column definitions that describe the table structure. |
| `data` | `TData[]` | - | Data records to render with the provided columns. |
| `density` | `'comfortable' \| 'compact'` | `'comfortable'` | Controls overall padding and typography density. |
| `enableSorting` | `boolean` | `true` | Toggles TanStack Table's sorting features. |
| `enableRowSelection` | `boolean` | `false` | Adds a selection column with Ariakit-powered checkboxes. |
| `activeRowId` | `string` | - | Marks a row as active for focus and highlighting. |
| `onActiveRowChange` | `(rowId: string \| undefined) => void` | - | Called when the active row changes. |
| `onRowClick` | `(row: Row<TData>) => void` | - | Fired when a table row is clicked. |
| `initialSorting` | `SortingState` | `[]` | Default sorting state when uncontrolled. |
| `sortingState` | `SortingState` | - | Controlled sorting state. |
| `onSortingChange` | `(sorting: SortingState) => void` | - | Handler used with controlled sorting. |
| `rowSelectionState` | `RowSelectionState` | - | Controlled selection state keyed by row id. |
| `onRowSelectionChange` | `(state: RowSelectionState) => void` | - | Handler used with controlled selection. |
| `onSelectedRowsChange` | `(rows: Row<TData>[]) => void` | - | Emits the currently selected rows array. |
| `emptyState` | `React.ReactNode` | - | Custom UI shown when there are no rows. |
| `getRowId` | `(originalRow: TData, index: number, parent?: Row<TData>) => string` | - | Supplies custom logic for deriving stable row ids. |
| `className` | `string` | `''` | Additional Tailwind classes merged with the component styles. |

The Table component also accepts all standard HTML `div` attributes.
