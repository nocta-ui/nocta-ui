---
title: Switch
description: A toggle switch component for binary choices with multiple variants, sizes and dark mode support
---

import { Switch } from '@/app/components/ui/switch';
import { 
  BasicSwitchDemo, 
  ControlledSwitchDemo, 
  VariantsDemo, 
  SizesDemo, 
  DisabledStateDemo,
  CustomStylingDemo
} from '@/app/components/ui/switch/switch-demos';
import { DocsTabs, DocsTab } from "@/app/components/ui/docs-tab"

## Installation

The Switch component is part of the nocta-ui component library. Import the component:

```tsx
import { Switch } from '@/app/components/ui';
```

## Basic Usage

### Default Switch

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <div className="my-6">
      <BasicSwitchDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx
    const [isChecked, setIsChecked] = useState(false);
    
    <Switch 
      checked={isChecked} 
      onCheckedChange={setIsChecked} 
    />
    ```
  </DocsTab>
</DocsTabs>

## Variants

The Switch component supports four variants: `default`, `success`, `warning`, and `destructive`.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <div className="my-6">
      <VariantsDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx
    <Switch variant="default" checked={true} />
    <Switch variant="success" checked={true} />
    <Switch variant="warning" checked={true} />
    <Switch variant="destructive" checked={true} />
    ```
  </DocsTab>
</DocsTabs>

## Sizes

Three size options are available: `sm`, `md`, and `lg`. The default size is `md`.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <div className="my-6">
      <SizesDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx
    <Switch size="sm" checked={true} />
    <Switch size="md" checked={true} />
    <Switch size="lg" checked={true} />
    ```
  </DocsTab>
</DocsTabs>

## Switch States

### Disabled State

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <DisabledStateDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx
    <Switch disabled={false} />
    <Switch checked={true} disabled={false} />
    <Switch disabled={true} />
    <Switch checked={true} disabled={true} />
    ```
  </DocsTab>
</DocsTabs>

## Customization

### Custom Styling

The Switch component accepts a `className` prop for custom styling:

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <div className="my-6">
      <CustomStylingDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx
    <Switch className="scale-125" checked={true} />
    <Switch className="opacity-75" checked={true} />
    <Switch className="rounded-sm" checked={false} />
    ```
  </DocsTab>
</DocsTabs>

## API Reference

### Switch Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | `false` | Whether the switch is checked |
| `onCheckedChange` | `(checked: boolean) => void` | - | Callback fired when the switch state changes |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | The size of the switch |
| `variant` | `'default' \| 'success' \| 'warning' \| 'destructive'` | `'default'` | The visual style variant |
| `disabled` | `boolean` | `false` | Whether the switch is disabled |
| `className` | `string` | `''` | Additional CSS classes to apply |
| `id` | `string` | - | The id attribute for the switch input |

The Switch component also accepts all standard HTML input element props through the `...props` spread operator. 