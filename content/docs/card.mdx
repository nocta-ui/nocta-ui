---
title: Card
description: A versatile card component system with modular architecture and dark mode support
---

import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, CardActions } from '@/app/components/ui/card';
import { Button } from "@/app/components/ui/button"
import { 
  SimpleCardDemo,
  CardWithActionsDemo,
  NotificationCardDemo,
  StatsCardDemo
} from '@/app/components/ui/card/card-demos';
import { DocsTabs, DocsTab } from "@/app/components/ui/docs-tab"

## Installation

Install the Card component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx nocta-ui add card
```

Then import the components you need:

```tsx lineNumbers title="Import"
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
  CardActions,
} from '@/components/ui/card';
```

## Basic Usage

### Simple Card

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SimpleCardDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Simple Card"
    <Card>
      <CardHeader>
        <CardTitle>Getting Started</CardTitle>
        <CardDescription>
          Learn how to use the Card component in your projects
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is a basic card example showing the fundamental structure.</p>
      </CardContent>
    </Card>
    ```
  </DocsTab>
</DocsTabs>

### Card with Actions

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <CardWithActionsDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Card with Actions"
    <Card>
      <CardHeader>
        <CardTitle>Project Settings</CardTitle>
        <CardDescription>
          Manage your project configuration and preferences
        </CardDescription>
      </CardHeader>
      <CardContent>
        <div className="space-y-3">
          <div className="flex items-center justify-between">
            <span className="text-sm font-medium">Status</span>
            <span className="px-2 py-1 text-xs font-medium bg-green-100 text-green-800 rounded-full">
              Active
            </span>
          </div>
        </div>
      </CardContent>
      <CardFooter>
        <CardActions>
          <Button variant="ghost" size="sm">Cancel</Button>
          <Button variant="primary" size="sm">Save Changes</Button>
        </CardActions>
      </CardFooter>
    </Card>
    ```
  </DocsTab>
</DocsTabs>

### Notification Card

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <NotificationCardDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Notification Card"
    <Card className="max-w-md">
        <CardHeader>
          <div className="flex items-start space-x-3">
            <div className="w-2 h-2 rounded-full mt-2 bg-black dark:bg-white"></div>
            <div className="flex-1">
              <CardTitle>System Update</CardTitle>
              <CardDescription>
                A new version is available
              </CardDescription>
            </div>
          </div>
        </CardHeader>
        <CardContent>
          <p className="text-sm">
            Version 2.1.0 includes performance improvements and bug fixes. 
            Update now to get the latest features.
          </p>
        </CardContent>
      </Card>
    ```
  </DocsTab>
</DocsTabs>

### Stats Card

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <StatsCardDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Stats Card"
      <Card className="w-md">
        <CardHeader>
          <CardTitle>Analytics Overview</CardTitle>
          <CardDescription>
            Key metrics for this month
          </CardDescription>
        </CardHeader>
        <CardContent>
          <div className="grid grid-cols-2 gap-4">
            <div className="text-center">
              <div className="text-2xl font-bold text-neutral-900 dark:text-neutral-100">2.4k</div>
              <div className="text-xs text-neutral-600 dark:text-neutral-400 uppercase tracking-wide">Users</div>
            </div>
            <div className="text-center">
              <div className="text-2xl font-bold text-neutral-900 dark:text-neutral-100">98%</div>
              <div className="text-xs text-neutral-600 dark:text-neutral-400 uppercase tracking-wide">Uptime</div>
            </div>
          </div>
        </CardContent>
      </Card>
    ```
  </DocsTab>
</DocsTabs>

## Customization

### Custom Styling

All components accept a `className` prop for custom styling:

```tsx lineNumbers title="Custom Styling"
<Card className="max-w-lg border-2 border-purple-200">
  <CardHeader className="bg-purple-50">
    <CardTitle className="text-purple-900">Custom Card</CardTitle>
  </CardHeader>
</Card>
```

### Dynamic Title Element

Use the `as` prop on CardTitle to change the heading level:

```tsx lineNumbers title="Dynamic Title Element"
<CardTitle as="h1">Main Title</CardTitle>
<CardTitle as="h2">Section Title</CardTitle>
<CardTitle as="div">Custom Element</CardTitle>
```