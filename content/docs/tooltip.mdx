---
title: Tooltip
description: A floating tooltip component that displays additional information on hover or focus with smart positioning and accessibility features
---

import { Tooltip, TooltipTrigger, TooltipContent } from '@/app/components/ui/tooltip';
import { Button } from '@/app/components/ui/button';
import { Badge } from '@/app/components/ui/badge';
import {
  SimpleTooltipDemo,
  WithTextTriggerDemo,
  DelayDurationDemo,
  TooltipPlacementDemo,
} from '@/app/components/ui/tooltip/tooltip-demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';

## Installation

import { Step, Steps } from 'fumadocs-ui/components/steps';

<Steps>
<Step>
Install the Tooltip component using the nocta-ui CLI:

```bash lineNumbers title="Terminal"
npx @nocta-ui/cli add tooltip
```
</Step>
<Step>
Import the components you need:

```tsx lineNumbers title="Import"
import {
  Tooltip,
  TooltipTrigger,
  TooltipContent,
} from '@/components/ui/tooltip';
```
</Step>
</Steps>

## Examples

### Default Tooltip

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <SimpleTooltipDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Simple Tooltip"
    <Tooltip>
      <TooltipTrigger>
        <Button variant="secondary">Hover me</Button>
      </TooltipTrigger>
      <TooltipContent>
        This is a helpful tooltip!
      </TooltipContent>
    </Tooltip>
    ```
  </DocsTab>
</DocsTabs>

### With Text Trigger

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <WithTextTriggerDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Tooltip with Text Trigger"
    <span>
      This text has a
      <Tooltip>
        <TooltipTrigger className="underline decoration-dotted underline-offset-4 cursor-help mx-1">
          tooltip
        </TooltipTrigger>
        <TooltipContent>
          Additional context information
        </TooltipContent>
      </Tooltip>
      that explains more.
    </span>
    ```
  </DocsTab>
</DocsTabs>

### Placement

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <TooltipPlacementDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Simple Tooltip"
    <Tooltip placement="top">
      <TooltipTrigger>
        <Button className="w-24" variant="secondary">
          Top
        </Button>
      </TooltipTrigger>
      <TooltipContent>Tooltip on top</TooltipContent>
    </Tooltip>

    <Tooltip placement="bottom">
      <TooltipTrigger>
        <Button className="w-24" variant="secondary">
        Bottom
        </Button>
      </TooltipTrigger>
      <TooltipContent>Tooltip on bottom</TooltipContent>
    </Tooltip>

    <Tooltip placement="left">
      <TooltipTrigger>
        <Button className="w-24" variant="secondary">
        Left
        </Button>
      </TooltipTrigger>
      <TooltipContent>Tooltip on left</TooltipContent>
    </Tooltip>

    <Tooltip placement="right">
      <TooltipTrigger>
        <Button className="w-24" variant="secondary">
        Right
        </Button>
      </TooltipTrigger>
      <TooltipContent>Tooltip on right</TooltipContent>
    </Tooltip>
    ```
  </DocsTab>
</DocsTabs>

### Delay Duration

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <DelayDurationDemo />
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Tooltip Delay Duration"
    <Tooltip delayDuration={0}>
      <TooltipTrigger>
        <Button size="sm" variant="ghost">Instant</Button>
      </TooltipTrigger>
      <TooltipContent>
        Appears immediately
      </TooltipContent>
    </Tooltip>

    <Tooltip delayDuration={1000}>
      <TooltipTrigger>
        <Button size="sm" variant="ghost">Slow</Button>
      </TooltipTrigger>
      <TooltipContent>
        Appears after 1 second
      </TooltipContent>
    </Tooltip>
    ```
  </DocsTab>
</DocsTabs>

## API Reference

### Tooltip

<TypeTable
  type={{
    delayDuration: {
      description: 'Delay before showing tooltip (ms)',
      type: 'number',
      default: '400',
    },
    placement: {
      description: 'Preferred tooltip placement relative to the trigger',
      type: '"top" | "bottom" | "left" | "right"',
      default: '"top"',
    },
    gutter: {
      description: 'Gap between the trigger and tooltip (px)',
      type: 'number',
      default: '0',
    },
    portal: {
      description: 'Whether tooltip content renders inside a portal',
      type: 'boolean',
      default: 'true',
    },
    fixed: {
      description: 'Use CSS `position: fixed` for the tooltip',
      type: 'boolean',
      default: 'false',
    },
    showArrow: {
      description: 'Render the arrow indicator for every tooltip content',
      type: 'boolean',
      default: 'true',
    },
    children: {
      description: 'Tooltip subtree',
      type: 'React.ReactNode',
      default: 'undefined',
    },
  }}
/>

### TooltipTrigger

<TypeTable
  type={{
    children: {
      description: 'Trigger content (element or text)',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional classes for the trigger wrapper',
      type: 'string',
      default: 'undefined',
    },
  }}
/>

### TooltipContent

<TypeTable
  type={{
    variant: {
      description: 'Visual style variant',
      type: '"default"',
      default: '"default"',
    },
    children: {
      description: 'Tooltip content',
      type: 'React.ReactNode',
      default: 'undefined',
    },
    className: {
      description: 'Additional classes for the content container',
      type: 'string',
      default: 'undefined',
    },
  }}
/>
