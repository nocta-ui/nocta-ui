---
title: Input
description: A versatile input component with multiple variants, sizes, icons, and dark mode support
---

import { Input } from '@/app/components/ui/input';
import { 
  DefaultInputDemo,
  InputWithLabelDemo,
  InputWithHelperTextDemo,
  VariantsDemo,
  SizesDemo,
  LeftIconDemo,
  RightIconDemo,
  DisabledStateDemo,
  ReadOnlyStateDemo,
  CustomContainerDemo
} from '@/app/components/ui/input/input-demos';
import { DocsTabs, DocsTab } from "@/app/components/ui/docs-tab"

## Installation

Install the Input component using the nocta-ui CLI:

```bash title="Terminal"
npx nocta-ui add input
```

Then import the component:

```tsx title="Import"
import { Input } from '@/components/ui/input';
```

## Basic Usage

### Default Input

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <DefaultInputDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Default Input"
    <Input placeholder="Enter your email" />
    ```
  </DocsTab>
</DocsTabs>

### Input with Label

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <InputWithLabelDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Input with Label"
    <Input 
      label="Email Address" 
      placeholder="Enter your email"
      type="email"
    />
    ```
  </DocsTab>
</DocsTabs>

### Input with Helper Text

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <InputWithHelperTextDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Input with Helper Text"
    <Input 
      label="Username" 
      placeholder="Choose a unique username between 3-20 characters"
      helperText="Choose a unique username between 3-20 characters"
    />
    ```
  </DocsTab>
</DocsTabs>

## Variants

The Input component supports three variants: `default`, `error`, and `success`.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <VariantsDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Input Variants"
    <Input 
      label="Default Input"
      placeholder="Default variant"
      variant="default"
    />
    
    <Input 
      label="Error Input"
      placeholder="Error variant"
      variant="error"
      errorMessage="This field is required"
    />
    
    <Input 
      label="Success Input"
      placeholder="Success variant"
      variant="success"
      helperText="Valid input"
    />
    ```
  </DocsTab>
</DocsTabs>

## Sizes

Three size options are available: `sm`, `md`, and `lg`. The default size is `md`.

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <SizesDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Input Sizes"
    <Input 
      label="Small Input"
      placeholder="Small size"
      size="sm"
    />
    
    <Input 
      label="Medium Input"
      placeholder="Medium size"
      size="md"
    />
    
    <Input 
      label="Large Input"
      placeholder="Large size"
      size="lg"
    />
    ```
  </DocsTab>
</DocsTabs>

## Input with Icons

### Left Icon

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <LeftIconDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Input with Left Icon"
    <Input 
      label="Search"
      placeholder="Search for anything..."
      leftIcon={<SearchIcon />}
    />
    
    <Input 
      label="Email"
      placeholder="Enter your email"
      type="email"
      leftIcon={<EmailIcon />}
    />
    ```
  </DocsTab>
</DocsTabs>

### Right Icon

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <RightIconDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Input with Right Icon"
    <Input 
      label="Password"
      placeholder="Enter your password"
      type="password"
      rightIcon={<EyeIcon />}
    />
    ```
  </DocsTab>
</DocsTabs>

## Input States

### Disabled State

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <DisabledStateDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Disabled Input"
    <Input 
      label="Disabled Input"
      placeholder="This input is disabled"
      disabled
    />
    
    <Input 
      label="Disabled with Icon"
      placeholder="Disabled with icon"
      disabled
      leftIcon={<EmailIcon />}
    />
    ```
  </DocsTab>
</DocsTabs>

### Read Only State

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <ReadOnlyStateDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx title="Read Only Input"
    <Input 
      label="Read Only Input"
      value="This value cannot be changed"
      readOnly
      helperText="This field is read-only"
    />
    ```
  </DocsTab>
</DocsTabs>

## Customization

### Custom Styling

The Input component accepts `className` and `containerClassName` props for custom styling:

```tsx title="Custom Styling"
<Input 
  className="border-2 border-purple-300 focus-visible:border-purple-500"
  containerClassName="max-w-xs"
  label="Custom Styled Input"
  placeholder="Custom styling"
/>

<Input 
  className="rounded-full"
  label="Rounded Input"
  placeholder="Fully rounded"
/>
```

### Custom Container

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" title="Preview">
    <CustomContainerDemo />
  </DocsTab>
  <DocsTab value="code" title="Code">
    ```tsx
    <Input 
      containerClassName="max-w-xs mx-auto"
      className="text-center"
      label="Centered Input"
      placeholder="Centered text"
    />
    ```
  </DocsTab>
</DocsTabs>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'default' \| 'error' \| 'success'` | `'default'` | Input style variant |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Input size |
| `label` | `string` | — | Label text displayed above the input |
| `helperText` | `string` | — | Helper text displayed below the input |
| `errorMessage` | `string` | — | Error message (shown when variant is 'error') |
| `leftIcon` | `React.ReactNode` | — | Icon displayed on the left side |
| `rightIcon` | `React.ReactNode` | — | Icon displayed on the right side |
| `className` | `string` | `''` | Additional CSS classes for the input |
| `containerClassName` | `string` | `''` | Additional CSS classes for the container |
| `disabled` | `boolean` | `false` | Disable the input |
| `readOnly` | `boolean` | `false` | Make the input read-only |
| `placeholder` | `string` | — | Placeholder text |
| `type` | `string` | `'text'` | Input type (text, email, password, etc.) |

The Input component also accepts all standard HTML input attributes.