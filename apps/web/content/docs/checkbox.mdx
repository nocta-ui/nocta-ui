---
title: Checkbox
description: A versatile checkbox component for forms and selections with multiple variants, sizes and dark mode support
---

import {
  BasicCheckboxDemo,
  CheckboxSizesDemo,
  CheckboxCustomStylingDemo
} from '@/demos';
import { DocsTabs, DocsTab } from "@/components/docs-tab"
import { TypeTable } from '@/components/type-table';
import { Step, Steps } from "fumadocs-ui/components/steps"

## Installation

<Steps>
  <Step>
    Install the Checkbox component using the nocta-ui CLI:

    ```bash lineNumbers title="Terminal"
    npx @nocta-ui/cli add checkbox
    ```
  </Step>
  <Step>
    Import the component:

    ```tsx lineNumbers title="Import"
    import { Checkbox } from '@/components/ui/checkbox';
    ```
  </Step>
</Steps>

## Examples

### Default Checkbox

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6">
      <BasicCheckboxDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Basic Checkbox"
    const [isChecked, setIsChecked] = useState(false);

    <Checkbox
      checked={isChecked}
      onCheckedChange={setIsChecked}
    />
    ```
  </DocsTab>
</DocsTabs>

### Sizes

<DocsTabs defaultValue="preview">
  <DocsTab value="preview" lineNumbers title="Preview">
    <div className="my-6">
      <CheckboxSizesDemo />
    </div>
  </DocsTab>
  <DocsTab value="code" lineNumbers title="Code">
    ```tsx lineNumbers title="Checkbox Sizes"
    <Checkbox size="sm" checked={true} />
    <Checkbox size="md" checked={true} />
    <Checkbox size="lg" checked={true} />
    ```
  </DocsTab>
</DocsTabs>

## API Reference

<TypeTable
  type={{
    checked: {
      description: 'Whether the checkbox is checked',
      type: 'boolean',
      default: 'false',
    },
    defaultChecked: {
      description: 'Initial checked state (uncontrolled)',
      type: 'boolean',
      default: 'false',
    },
    onCheckedChange: {
      description: 'Callback fired when the checkbox state changes',
      type: '(checked: boolean) => void',
      default: 'undefined',
    },
    size: {
      description: 'The size of the checkbox',
      type: '"sm" | "md" | "lg"',
      default: '"md"',
    },
    disabled: {
      description: 'Whether the checkbox is disabled',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes to apply',
      type: 'string',
      default: '""',
    },
    id: {
      description: 'The id attribute for the checkbox input',
      type: 'string',
      default: 'undefined',
    },
  }}
/>
